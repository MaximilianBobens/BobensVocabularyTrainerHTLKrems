namespace BlazorApp7;

public class ProgressService
{
    public void SaveProgress(int year, int unit, int learnedWords)
    {
        // Logik, um den Fortschritt zu speichern
    }

    public List<Progress> LoadProgress()
    {
        // Logik, um gespeicherte Fortschritte zu laden
        return new List<Progress>();
    }
}